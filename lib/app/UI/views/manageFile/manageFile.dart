import 'package:flutter/material.dart';

class ManageFile extends StatelessWidget {
  const ManageFile({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text(
          'Manage all the file you have uploaded',
        ),
      ),
    );
  }
}
